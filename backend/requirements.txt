# API layer - pinned to exact versions verified together: server boots, CORS confirmed
# against a real cross-origin request, and the full pytest suite (30 tests) passes.
fastapi==0.138.1
starlette==1.3.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
motor==3.7.1
pymongo==4.17.0
python-multipart==0.0.32
python-dotenv==1.0.1
aiofiles==25.1.0

# Image processing - pinned to versions verified via the image_utils test suite.
pillow==12.2.0
opencv-python-headless==5.0.0.93
numpy==2.4.6

# ML training/inference (ml_model/train.py) - untested in this environment (TensorFlow
# is a large install); left as minimum-version bounds rather than exact pins so pip's
# resolver can pick versions mutually compatible with numpy 2.x above instead of being
# forced into a combination nobody has actually verified.
tensorflow>=2.18,<2.21
scikit-learn>=1.5,<1.8
matplotlib>=3.9,<3.11
seaborn>=0.13,<0.14

# Testing
pytest==9.1.1
httpx==0.28.1
